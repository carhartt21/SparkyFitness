# PersonalBest physical-device rebrand gate (historical)

This records tests for the prior identity. The current [X on Track release gate](x-on-track-rebrand.md#release-checks) requires new asset and device evidence.

Date: 24 September 2026. Device: iPhone 17 Pro running iOS 27.2. Build host: Xcode 26.6 on Apple silicon. Tests use the existing synthetic local-first test app and isolated API; no production service or personal health data is involved.

| Check                                                           | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Physical device paired, unlocked, Developer Mode available      | Passed (`devicectl` connected).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Same test bundle ID and App Group as prior synthetic test app   | Passed; generated development configuration matches its existing provisioning profiles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Signed iPhone Release build, including widget and Watch targets | Passed (`xcodebuild` and `codesign --verify --deep --strict`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| In-place install and process launch                             | Passed (`devicectl`); previous app was not uninstalled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Built app and extension display names                           | Passed: PersonalBest, PersonalBest Widgets, PersonalBest Watch Widgets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Dark and tinted iOS app icon renditions                         | Present in the signed asset catalog. Visual device review pending.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Isolated test API on current Tailscale address                  | Passed: version endpoint reports `1.7.2` on Mac and iPhone Safari.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Synthetic diary records and photos retained after upgrade       | Passed: owner opened the signed PersonalBest build and confirmed the earlier TEST diary data is present.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Development test server URL and sign-in path                    | Passed: the one-host private test origin is accepted and the synthetic diary opens after configuring the test server. This HTTP allowance is confined to the signed local test build.                                                                                                                                                                                                                                                                                                                                                               |
| First on-device light/dark visual review                        | Found: low contrast on filled green controls in dark appearance, an outdated oversized Dashboard hydration card, and old assistant wording in What's New/About. Corrective source changes now pass mobile validation; a second device pass is pending.                                                                                                                                                                                                                                                                                                  |
| Food-entry note editing                                         | Device finding: Save remained reachable and the note persisted, but the text field disappeared behind the keyboard while typing. Both forms recheck the note's visible position as the keyboard and draft change. The add-entry native modal now has its own keyboard provider, and both editor scroll views reserve keyboard space with a layout spacer. On-device retest after a confirmed reload is pending. Formatting controls are hidden for diary food entries.                                                                                  |
| Hydration quick log and reminder arbitration                    | Source and automated tests cover the account-bound “Log 250 ml” response, durable offline action, local/server operation-ID reconciliation for plain-water and container taps, an immediate reminder anchor for queued container taps without estimated volume, a durable three-attempt local-day ledger shared with meal and movement prompts, a Settings preview of today's used slots, and spacing around pending medication base/repeat requests. On-device delivery, offline replay, account switching, and visible reconciliation remain pending. |
| Planned supplement reminder response                            | Source and focused tests cover account-scoped offline queueing, repeated-tap operation identity, server idempotency after entry deletion, diary permission, queued-dose display, reminder suppression, and review-and-clear for rejected responses. Migration and RLS applied on a local isolated PostgreSQL test database; use a migrated API database for device testing. On-device notification delivery, airplane-mode queueing, relaunch/replay, account switching, and pending/conflict rendering remain pending.                                 |
| Fasting control                                                 | A new App Settings switch now gates the Dashboard fasting card and fasting-goal reminders without deleting recorded fasts. Device verification pending.                                                                                                                                                                                                                                                                                                                                                                                                 |
| Home/Lock widgets, Live Activities, Watch                       | Existing signed targets compiled; visual/device behavior pending. Do not infer these from build success.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Watch workout page and set actions                              | The Watch target compiles with the active-session mirror and persisted set-action outbox. Verify on paired devices that set values, completion, cursor, and rest countdown update; offline taps deliver in order, duplicates do not repeat, conflicts are visible, and ending a session clears the page.                                                                                                                                                                                                                                                |
| Opt-in movement-break reminder                                  | Policy and notification-response tests pass. The timer offers an explicit link to the existing Activity form; starting or finishing it never saves an exercise entry. On-device delivery, account-scoped tap to timer, Activity-form route, and cancellation after starting a break remain pending.                                                                                                                                                                                                                                                     |

Hevy history display gate: import a synthetic completed workout with a configured exercise rest interval, warm-up and separate drop-set rows, and a superset. On the phone, confirm the diary shows every set in order with its type and rest interval, and that the saved-routine library contains only routines supplied by the separate API fixture. This gate is pending; the server mapping test does not prove mobile presentation.

Routine widget gate: add the new Routines widget to the iPhone Home Screen and Lock Screen after a rebuilt app is installed. The Home widget should show the account-visible routine count and open the routine library; the Lock accessory should show only a generic shortcut. Switch between two synthetic accounts and verify the prior count disappears before the new account's count is fetched. Let a snapshot age past 24 hours and verify it asks to refresh. The full unsigned iOS simulator build passes; these device observations remain pending.

Watch quick-water gate: install a rebuilt phone and Watch app, sync a synthetic signed-in account, and verify Quick water appears beside configured containers. Tap 250 ml with the phone online and confirm one server water entry and Watch acknowledgement. Repeat with the phone API offline: the Watch should show a queued action, retain it after relaunch, and send the same operation ID when connectivity returns; the server should still show only one entry after duplicate delivery. Switch the phone account before a queued delivery and verify the old scope is rejected without writing water to the new account. Check the Watch's VoiceOver label and compact layout. Separately, tap a linked drink on the phone while the API is offline and verify a pending drink count appears without inflating the confirmed water total; the reminder should use that tap time without adding an unconfirmed volume. Reconnect and confirm exactly one linked food/water pair and that the pending count clears when the server log exposes the same operation ID, including after a lost response. The source and unsigned simulator build pass, but none of these paired-device observations has been made.

Hydration recovery gate: with a synthetic saved manual-water or container action marked “needs attention,” tap the Dashboard retry control. Confirm the app retries the same operation ID and day, never creates a second drink, and keeps a rejected action visible if the server still rejects it. Verify the control is absent when only ordinary pending water exists. Focused source tests pass; the device observation is pending.

Nutrition widget and Watch account-switch gate: with a rebuilt app, add the Nutrition widget using synthetic account A. Queue a food entry, a water action, and a planned supplement under A; switch to synthetic account B and verify A's queued rows, favorites, and widget counts disappear before B's data is ready. Open a quick calories draft under A and verify it closes and clears on switching to B; verify A's hydration reminder chain is cancelled and any B chain waits for B's water data. Sign out and verify no account data remains visible. On the paired Watch, verify A's water containers, goal, unit, check-in status, queued water, workout action state, and energy/water complication rings disappear under B. Capture one check-in and one container tap under A while the phone is offline; switch the phone to B before delivery and verify neither is written to B. Switch back to A and verify the queued check-in retries and the water tap remains available for retry under A. Repeat for an unsent set action. The scope checks, Swift behavior checks, and clean unsigned simulator build pass; widget redraw timing, WatchConnectivity delivery, and on-wrist presentation remain device observations.

Watch first-sync gate: before the Watch receives a signed-in account scope from the phone, verify the first-run check-in asks for phone sync and cannot save; on the regular entry page, Save and Skip body fat must remain unavailable, and a container square must not create a local tap. Once the phone context arrives, these controls should become usable. On the first-run form, verify `nan`, `inf`, zero weight, and a nonempty invalid body-fat value keep Save disabled; blank body fat should remain optional. Existing unscoped captures from older builds must not be assigned to the currently signed-in account.

The updated Watch target passed an unsigned simulator build. Its first-sync prompt and Retry sync button rendered without clipping on the 40 mm Apple Watch SE 3 simulator. This checks the smallest layout only; it does not prove phone context delivery, input validation interaction, or paired-device behavior.

Watch water rollover gate: using a synthetic container, tap shortly before midnight with the phone out of range. After midnight, today's bottle must not include that tap, but the pending/failed indicator must remain. If it fails, retry and verify the server records the drink against the original day. With the rebuilt app and migrated API, deliver the same Watch tap twice after a lost acknowledgement and confirm the server returns the same water and linked-food IDs with only one diary effect. The source and local database checks do not establish paired-device delivery.

The transactional container-action migration is present in the isolated local database. The development API was restarted on 24 September 2026 and `/api/version/current` returned 200 with the new route loaded. A container tap from the Watch now enters the phone's account-scoped durable outbox before the API call; the same operation ID is retried after a lost response or phone relaunch, and the Watch retains its queued tap until acknowledgement. The rebuilt phone and Watch targets passed an unsigned iOS simulator build. Paired-device delivery and the food-note visibility retest still need direct observation; the latter only needs a confirmed Expo JavaScript reload.

A separate iPhone 17 Pro simulator Release build also compiled. Its installed Home Screen shows the PB icon and PersonalBest label after SpringBoard finishes refreshing the icon cache. The simulator app then exits in HealthKit while creating its default source (`client bundle identifier is nil`) under an unsigned simulator build, so it cannot provide a trustworthy in-app visual review. The signed physical-device build previously launched successfully; the revised build still requires a second device pass.

Phone-free validation of the corrective changes: all 454 mobile Jest suites passed (7,046 tests), as did TypeScript, Expo lint, i18n generation/audit, native widget locale checks, Knip, Prettier, and `git diff --check`. The green selection controls now use a semantic foreground; the dark filled-accent foreground measures 7.66:1 against its background, and muted card copy measures 7.49:1. These static checks do not replace visual inspection on the iPhone. The final signed iOS Release build passed and `codesign --verify --deep --strict` accepted the app. Its display name is PersonalBest and its test bundle identifier is unchanged. Installation and the second visual pass remain pending while the phone is unavailable.

The isolated API had been bound to a former Tailscale address. It was moved to the Mac's current private address and the stale listener stopped. The device build uses a one-host development App Transport Security exception for that isolated API. Production HTTPS configuration and the signed local test build's identifiers were not changed. The local native project was generated with temporary test-only environment overrides; regenerate it from the normal repository configuration before a different build or distribution.

The separate EAS internal-build project is now verified by `eas project:info` as `@ilmtech/personalbest` (`d0d1c7d2-deb1-4675-9411-2a1bd0a3a967`). The local app configuration uses that ID and the `personalbest` slug; the existing `sparkyfitnessmobile://` deep-link scheme stays in place. The `development` profile uses internal distribution. Apple rejected the previous repository development bundle identifier, so the owner selected `com.cg.phi`; the derived Watch/widget identifiers and development App Group now use that prefix. The Android development package stays unchanged. Its archive inspection passed. After the owner completed interactive Apple signing for all five targets, EAS build `c2d6f110-bc7f-4001-ab77-6ce4b96ebc63` finished successfully. The owner explicitly approved replacing the unrelated HealthIntel app that was installed under `com.cg.phi`; the EAS IPA was installed and its process launched on the connected iPhone. The signed local Xcode build above used `de.ilmtech.sparkyfitness.localfirsttest` and remains installed for in-place checks. Expo Doctor currently reports SDK patch mismatches, duplicated pnpm native installations, and a `knip` script name conflict; these are build-readiness findings, not physical-device results.

The owner subsequently signed in through the EAS development build using a temporary HTTPS Expo tunnel and confirmed that previous entries are visible. An initial sign-in attempt returned Expo's 500 CORS error because the isolated API advertised its HTTP origin; the development-only Metro proxy now advertises the tunnel origin to the app and translates it back for the isolated API. A synthetic invalid-credentials request reached Better Auth and returned its normal 401 response. The owner also tested a long food-entry note on the iPhone: Save stayed reachable and the note persisted after saving, but the field was covered by the keyboard while typing. A follow-up still found it hidden, but Expo reported no connected app, so it is unclear whether the revised JavaScript loaded. The add and edit forms now measure the note in screen coordinates after the keyboard opens, when the keyboard frame changes, and as the draft grows. Both scroll views now reserve keyboard space with the controller's layout spacer; the add-entry modal also has a local keyboard provider. The add form only requests a measured scroll when the note overlaps the keyboard or Save bar; scrolling to the absolute end could move the note off-screen. The edit input is capped at 144 points so a long note scrolls inside its field. Both screen tests, TypeScript, lint, and formatting pass. Physical verification of this revised code remains pending.

The note visibility hook rechecks both forms after keyboard appearance and as content height changes. After a report that the field was still hidden, the add-entry modal gained a local keyboard provider and both editor scroll views switched to a layout spacer. The two focused screen suites passed (40 tests), as did full mobile validation and the UI detector. The on-device result is still pending.

For the pending retest, open the development build while the secure Expo tunnel is running, use the in-app development menu's **Reload** action, and wait for the PersonalBest splash/loading state and return to the screen. Confirm the Expo terminal reports a connected app and reload request before judging the new JavaScript. A reload command issued while Expo reports “No apps connected” is not evidence that the phone received the change. Then enter a long note with the keyboard open, check the caret and text remain above the keyboard, dismiss it, save, and reopen the entry to check persistence.

The latest source revision replaces the note hook's JavaScript scroll-offset calculation with React Native's measured scroll-to-keyboard method. The keyboard-aware view can move on the UI thread before its `onScroll` event reaches JavaScript; reusing that stale offset could move the note back beneath the keyboard. The focused add/view screen and hook tests, TypeScript, and touched-file lint pass. This remains a source-level correction until the confirmed-reload phone test above shows the field and caret staying visible.

Second device pass when the iPhone is available: install the signed build **in place**, open the existing synthetic diary, inspect Dashboard and Settings in light and dark appearance, exercise the Dashboard hydration card with a synthetic configured container, confirm What's New/About show PersonalBest wording, edit a synthetic food-entry note without a formatting bar, and toggle fasting off/on to verify the Dashboard card hides and returns without changing an existing record. Check that the prior TEST photos remain present. With an updated build, enable the movement-break reminder at a near-future time, verify one delivery opens the timer without starting/logging a session, then start a break and confirm the prompt is cleared; repeat after switching test accounts. A passing build or simulator Home Screen cannot substitute for these on-device observations.
