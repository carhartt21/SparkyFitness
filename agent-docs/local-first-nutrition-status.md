# Local-first nutrition status — 2026-09-23

This work is on `feat/local-first-nutrition`, based on upstream v1.7.2. It has not been deployed to the production SparkyFitness server. A separate signed test build is installed on an iPhone; HealthIntel and its stored data remain untouched.

## Implemented

- Server food creation accepts an optional UUID `client_operation_id`. The database has a partial unique index on `(user_id, client_operation_id)`; retries return the first snapshot. Existing web requests remain valid. Standalone offline snapshots have a narrow owner-scoped RLS policy.
- The mobile outbox stores versioned, owner/server-partitioned actions in AsyncStorage. It persists food logs, photo captures, and manual photo completions before any network request. Its coordinator retries bounded batches on startup, foreground, and connectivity recovery after verifying the authenticated owner. The diary projects local food and photo actions before sync and matches completed server rows by operation or capture ID.
- Favorites and meal types are cached locally. A cached favorite's default portion and nutrients can be logged offline. Quick calorie/macro entry uses the same action path.
- Meal photos are copied into application documents before the capture action is stored. The server stores an incomplete capture and authenticated image attachments separately from food entries. Missing nutrition is absent, not zero; the diary counts incomplete captures. A manual reviewed snapshot can complete a capture while keeping its ID, photo, and timestamps. Completion uses another stable operation UUID and one linked food entry.
- Local and provider search rows show energy and macros per 100 g or 100 ml only for explicit metric mass/volume servings. Unknown nutrients display as unknown; package servings remain labeled per serving.
- Existing calorie and macro iOS widgets now link directly to the meal-photo route, which hands capture to the mobile outbox. The widget does not write to the server.

## Schema and API

Migrations, in order: `20260923000000_add_food_entry_client_operation_id.sql`, `20260923020000_add_nutrition_captures.sql`, and `20260923030000_link_food_entries_to_nutrition_captures.sql`. Owner-only capture and standalone-snapshot policies live in `db/rls_policies.sql`, which startup reapplies after migrations. Apply to a disposable copy of the previous stable database before production rollout.

`POST /api/food-entries/` may include `client_operation_id`; `(user_id, client_operation_id)` identifies one immutable create operation. `POST /api/nutrition-captures` uses the capture UUID as its idempotent create key. `PUT /api/nutrition-captures/:id/images/:imageId` uses the image UUID. `POST /api/nutrition-captures/:id/complete` requires a distinct `clientOperationId` and reviewed food snapshot, links one food entry to the capture, and returns the existing result on retry. A second completion operation is rejected. The capture remains the photo occurrence and holds captured/consumed instants; the food row supplies daily nutrient totals. The current completion API supports one food snapshot per capture.

## Release gates and limits

- Full validation passed for server and mobile. Server tests: 399 files passed, 7 skipped; 4,862 tests passed, 305 skipped after the RLS fix. Mobile tests: 448 suites and 7,006 tests passed. Targeted favorite-cache tests passed after nullable nutrient normalization. A fresh disposable PostgreSQL 18 database completed every migration and a repeat startup kept all three nutrition policies. A second disposable database was initialized from the previous stable revision, seeded with a legacy food entry, and migrated forward; the row retained its ID, timestamp, and nutrient values after a repeat startup. Synthetic two-user checks confirmed owner write/read and cross-owner denial for captures and linked food. The iOS prebuild generated targets, the `CalorieTracker` widget target built, and the full `SparkyFitness` app scheme built for the iOS 26.2 simulator with signing disabled. A signed standalone build was installed on an iPhone 17 Pro for the synthetic outage tests described below.
- The production server is still on its prior release. Deploy server migrations and matching API before distributing a new mobile build. Test the migrations on a restorable database copy and test auth/RLS with two users.
- Photo completion currently supports manual kcal/macros. Search and AI completion into the *same* capture are still pending. AI remains optional and is not invoked during capture.
- Native App Intents, direct favorite/quick-entry widget actions, Lock Screen accessories, and Watch nutrition logging remain pending. The current widget action opens the app camera; it does not itself save a record until the user captures a photo.
- Favorite meals and recent meals are not cached for offline logging. Existing online editing/deletion is unchanged. Photo deletion UI, local file cleanup after deletion, startup orphan cleanup, and backup/export inclusion need follow-up. A permanently rejected completion remains in the outbox for attention rather than being discarded.
- Pending food and completed-photo snapshots are visible in local diary cards. The diary aggregate overlays their known nutrients until server rows become visible; disconnected mode shows only a clearly labeled device-known subtotal. Widget snapshots still come from the server and do not include local pending values.
- A disposable local test server now supports a restricted bind through `SPARKY_FITNESS_SERVER_BIND_HOST`; the default container bind behavior is unchanged. Synthetic API checks passed food/capture/image/completion retry identity, private photo access, and server-side capture/image deletion. The Mac's active address is public, so this server is bound only to its existing Tailscale address.
- The iPhone 17 Pro ran a signed, standalone test build with temporary, unique bundle/app-group IDs. It signed in to the private synthetic backend, opened the camera through the direct meal-photo route, and saved a synthetic photo that appeared once in the diary as incomplete. An initial rapid save failed before retry succeeded; onboarding now primes the nutrition identity before navigation, with a fresh-device retest pending. The test build has one exact HTTP origin exception for the encrypted Tailscale connection; the normal production variant retains its HTTPS-only policy. With the isolated API deliberately stopped, a new photo appeared incomplete and survived force-quit and reopen with its original time. On API recovery it synchronized as one capture with one image and the original instant; replaying the capture ID did not add a row. Manual completion on the phone preserved the capture ID, timestamp, and image, added one synthetic food snapshot, and an authenticated completion retry did not duplicate it.
- The first offline favorite attempt exposed a cache validation failure: the API returned JSON `null` for unknown optional nutrients. The corrected build omits those unknown fields while retaining numeric zero, and it is installed on the iPhone. Its local cache held the synthetic `TEST PROTEIN BAR` 1-bar snapshot. With the isolated API stopped, two intentional taps on the Diary quick-action button produced two distinct pending operations and two device-only diary rows. Both survived force-quit/reopen; the server still had zero bar rows during the outage. On recovery the server stored exactly two rows with the original local times and 200 kcal / 20 g protein / 20 g carbohydrate / 5 g fat snapshots. Replaying both operation IDs returned the same records, leaving two database rows. The owner confirmed exactly two diary rows after sync and another online force-quit/reopen, without pending duplicates.
- The repeat-camera issue was reproduced on the iPhone. Launching the system camera now waits for the native-stack modal transition to finish, with a fallback for cold launch. Four focused tests and mobile validation pass. The updated build opened the camera twice in succession after the owner canceled the first launch.
- A `devicectl` cold launch through `sparkyfitnessmobile://meal-photo` opened the camera directly on the physical iPhone with Airplane Mode on, Wi‑Fi off, and the isolated API stopped. The synthetic photo saved incomplete, survived an offline force-quit/reopen with image and original capture time, and synchronized after reconnection as the same capture UUID with one server image and one diary row.
- A Home Screen widget Meal photo tap also opened the camera while the isolated API was stopped. The new incomplete photo survived force-quit/reopen offline. After reconnection, the owner manually completed it as `Test`, 200 kcal; the server retained the original capture UUID and time, one image, and one linked food row, with no second meal lineage. A separate widget tap from a force-quit cold state opened the camera directly; the owner canceled without saving.
- On-device review confirmed that photo completion currently offers a manual nutrition form but no handoff into the existing food search. Search-based completion of the same capture remains a follow-on implementation item.
- The first immediate photo save after initial sign-in failed once before retry; identity priming was added and unit-tested. A clean-device sign-in retest is still pending because the installed mobile build has no sign-out control. No existing test app data was removed to force onboarding.
- The completion form has only English source strings for its new copy, with localization fallback; native meal-photo widget labels are localized in English and German, with English fallback elsewhere.

## HealthIntel migration readiness

No historical data has been imported. Map HealthIntel supplement definitions, schedules, and actual intake events separately. Preserve original source IDs, instants, timezone context, units, missing values, and attachments. Import into a disposable SparkyFitness namespace first; report unmapped records and collisions, then use stable source-derived operation IDs for idempotent repeat imports. Archive analytics artifacts and any record without a faithful destination rather than flattening them into food intake. Do not shut down the HealthIntel runtime or change its keys as part of this branch.
